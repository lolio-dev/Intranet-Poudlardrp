export enum InstanceTypes {
  mmo = 'mmo',
  lobby = 'lobby',
  sandwich_master = 'sandwich_master',
  sandwich_slave = 'sandwich_slave',
  proxy = 'proxy',
  webapi = 'webapi',
  pilote = 'pilote',
}
